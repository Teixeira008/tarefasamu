#include <stdio.h>
#include <ctype.h>





int main(){
    char vogais[] = {'a', 'e', 'i', 'o', 'u'};

    int ditado;

    printf("escreva um ditado:");
    scanf("%d", "%d", &ditado, &vogais);

    for (int i = 0; vogais != 0; i++)
    {
        
    }
    

}